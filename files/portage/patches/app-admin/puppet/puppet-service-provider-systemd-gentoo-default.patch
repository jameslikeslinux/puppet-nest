diff --git a/lib/puppet/provider/service/init.rb b/lib/puppet/provider/service/init.rb
index a28c4b8e52..308b4c08b0 100644
--- a/lib/puppet/provider/service/init.rb
+++ b/lib/puppet/provider/service/init.rb
@@ -24,7 +24,7 @@ Puppet::Type.type(:service).provide :init, :parent => :base do
   confine :true => begin
       os = Puppet.runtime[:facter].value('os.name').downcase
       family = Puppet.runtime[:facter].value('os.family').downcase
-      !(os == 'debian' || os == 'ubuntu' || family == 'redhat')
+      !(os == 'debian' || os == 'ubuntu' || family == 'redhat' || family == 'gentoo')
   end
 
   # We can't confine this here, because the init path can be overridden.
diff --git a/lib/puppet/provider/service/systemd.rb b/lib/puppet/provider/service/systemd.rb
index abc1a2653b..a36e448ea8 100644
--- a/lib/puppet/provider/service/systemd.rb
+++ b/lib/puppet/provider/service/systemd.rb
@@ -12,8 +12,7 @@ Puppet::Type.type(:service).provide :systemd, :parent => :base do
 
   commands :systemctl => "systemctl"
 
-  confine :true => Puppet::FileSystem.exist?('/proc/1/comm') && Puppet::FileSystem.read('/proc/1/comm').include?('systemd')
-
+  defaultfor 'os.family' => :gentoo
   defaultfor 'os.family' => [:archlinux]
   defaultfor 'os.family' => :redhat, 'os.release.major' => ["7", "8", "9"]
   defaultfor 'os.family' => :redhat, 'os.name' => :fedora
