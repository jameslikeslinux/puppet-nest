---
nest::package_env:
  # Compiler deadlocks under QEMU
  dev-lang/ghc:
    env: 'no-makeopts.conf'
  dev-lang/python:
    env: 'no-makeopts.conf'

nest::kernel_config:
  # Unset default preempt for ZFS compatibility
  CONFIG_PREEMPT: n
  CONFIG_PREEMPT_VOLUNTARY: y
