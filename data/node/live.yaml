---
nest::fscache: false
nest::live: true

# Try to be readable on a wide variety of screens
nest::text_scaling_factor: 1.25

# xorriso makes GPT-compatible ISO images using an HFS+ filesystem
nest::kernel_config:
  CONFIG_HFSPLUS_FS: y
