---
grafana_service_name: grafana-test

values:
  # Use existing cluster scrape services
  coreDns:
    service:
      enabled: false
  kubeControllerManager:
    service:
      enabled: false
  kubeEtcd:
    service:
      enabled: false
  kubeProxy:
    service:
      enabled: false
  kubeScheduler:
    service:
      enabled: false
  kubelet:
    service:
      enabled: false
  prometheusOperator:
    kubeletService:
      enabled: false
