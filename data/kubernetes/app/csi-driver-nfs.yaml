---
resources:
  # Disable Nest backup
  backup: {}

  storageclass:
    apiVersion: storage.k8s.io/v1
    kind: StorageClass
    metadata:
      name: eagle
    provisioner: nfs.csi.k8s.io
    parameters:
      server: eagle.nest
      share: /export
