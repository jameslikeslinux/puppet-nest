---
nest::package_use:
  # Don't pull in X
  dev-java/icedtea-bin:
    use: '-gtk'

  # For networkmanager[wifi]
  net-wireless/wpa_supplicant:
    use: 'dbus'

  # Don't pull in X
  sys-boot/plymouth:
    use: '-gtk'
